#include "CSVParser.hpp"
#include <map>

namespace PlaceDataLib
{
        Data CSVParser::parsePlaceCSV(std::string filepath, std::unordered_map<dataFormat, bool (*)(Placement)> rules)
        {
            Data data;

            return data;
        }

} // End Namespace